import Image from "next/image";
import Link from "next/link";
import {
  FacebookIcon,
  HeadphonesIcon,
  InstagramIcon,
  LinkedinIcon,
  ShieldIcon,
  StarIcon,
  TruckIcon,
} from "@/components/icons/LineIcons";

const featureCallouts = [
  {
    label: "Trusted Quality",
    description: "Premium products you can rely on",
    icon: ShieldIcon,
  },
  {
    label: "Top Rated",
    description: "Loved by thousands of happy customers",
    icon: StarIcon,
  },
  {
    label: "24/7 Support",
    description: "We're here whenever you need us",
    icon: HeadphonesIcon,
  },
  {
    label: "Fast Delivery",
    description: "Quick. Reliable. Right to your door.",
    icon: TruckIcon,
  },
];

const footerColumns = [
  {
    title: "Shop",
    links: [
      { label: "Shop All", href: "/category/all" },
      { label: "Categories", href: "/category/all" },
      { label: "Wishlist", href: "/wishlist" },
    ],
  },
  {
    title: "Company",
    links: [
      { label: "About Us", href: "/about" },
      { label: "Services", href: "/services" },
      { label: "Contact", href: "/contact" },
    ],
  },
  {
    title: "Support",
    links: [
      { label: "Help & Support", href: "/support" },
      { label: "Track Order", href: "/track-order" },
      { label: "Account", href: "/account" },
    ],
  },
  {
    title: "Legal",
    links: [
      { label: "Privacy Policy", href: "/privacy" },
      { label: "Terms & Conditions", href: "/terms" },
      { label: "Shipping & Returns", href: "/shipping-returns" },
    ],
  },
];

export function Footer() {
  return (
    <footer className="bg-ink text-white">
      <div className="site-container">
        <div className="grid gap-8 border-b border-white/15 py-11 desktop:grid-cols-[1fr_220px] desktop:items-center desktop:py-12">
          <div className="grid grid-cols-1 gap-8 tablet:grid-cols-2 desktop:grid-cols-4 desktop:gap-6">
            {featureCallouts.map(({ label, description, icon: Icon }) => (
              <div className="flex items-start gap-4" key={label}>
                <Icon className="mt-0.5 shrink-0 text-gold" />
                <div>
                  <p className="text-[15px] font-semibold text-white">{label}</p>
                  <p className="mt-1 text-caption leading-5 text-white/65">{description}</p>
                </div>
              </div>
            ))}
          </div>

          <Image
            className="h-auto w-[190px] justify-self-start desktop:justify-self-end"
            src="/brand/logo-reversed.png"
            alt="Wow & Amazing"
            width={1081}
            height={551}
          />
        </div>

        <div className="grid gap-10 py-11 desktop:grid-cols-[1fr_340px] desktop:gap-16 desktop:py-14">
          <div className="grid grid-cols-2 gap-x-8 gap-y-10 tablet:grid-cols-4">
            {footerColumns.map((column) => (
              <div key={column.title}>
                <h2 className="text-[13px] font-semibold uppercase tracking-[0.12em] text-white">
                  {column.title}
                </h2>
                <ul className="mt-5 space-y-3">
                  {column.links.map((link) => (
                    <li key={link.label}>
                      <Link className="footer-link" href={link.href}>
                        {link.label}
                      </Link>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>

          <div>
            <p className="text-[13px] font-semibold uppercase tracking-[0.12em] text-white">Newsletter</p>
            <p className="mt-4 text-body-mobile text-white/65">Receive product news, service updates, and curated offers.</p>
            <form className="mt-5 flex flex-col gap-3 tablet:flex-row" action="#">
              <label className="sr-only" htmlFor="newsletter-email">
                Email address
              </label>
              <input
                className="input-field min-w-0 flex-1 border-white/25 bg-white text-ink placeholder:text-warm-grey"
                id="newsletter-email"
                name="email"
                type="email"
                placeholder="Email address"
                autoComplete="email"
              />
              <button className="button-primary shrink-0" type="submit">
                Subscribe
              </button>
            </form>

            <div className="mt-6 flex items-center gap-2" aria-label="Social links">
              <a className="social-link" href="#" aria-label="Instagram">
                <InstagramIcon />
              </a>
              <a className="social-link" href="#" aria-label="Facebook">
                <FacebookIcon />
              </a>
              <a className="social-link" href="#" aria-label="LinkedIn">
                <LinkedinIcon />
              </a>
            </div>
          </div>
        </div>

        <div className="border-t border-white/15 py-6 text-caption text-white/55">
          © 2026 Wow &amp; Amazing. All rights reserved.
        </div>
      </div>
    </footer>
  );
}
