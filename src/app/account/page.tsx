import { AccountClient } from "@/components/account/AccountClient";

export default function AccountPage() {
  return (
    <section className="section-shell store-page premium-account-page">
      <div className="site-container account-page-shell">
        <AccountClient />
      </div>
    </section>
  );
}
